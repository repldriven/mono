{
  "name": "bank-app",
  "private": true,
  "type": "module",
  "scripts": {
    "cljs:compile": "npx cherry compile src/lib/api.cljs --output-dir src --extension mjs",
    "dev": "npm run cljs:compile && vite",
    "build": "npm run cljs:compile && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^5.0.3",
    "cherry-cljs": "^0.5.34",
    "svelte": "^5.25.3",
    "vite": "^6.2.2"
  }
}
