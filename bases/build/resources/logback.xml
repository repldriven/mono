<?xml version="1.0" encoding="UTF-8"?>
<!-- The build tool's own logging. Without it, the dependency resolver logs
     every lock and repository lookup at DEBUG over a build's output. A
     logback-test.xml on the classpath still wins, so this changes no test
     run. -->
<configuration>
    <statusListener class="ch.qos.logback.core.status.NopStatusListener"/>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%-5level %logger{0} - %msg%n</pattern>
        </encoder>
    </appender>

    <root level="warn">
        <appender-ref ref="CONSOLE"/>
    </root>
</configuration>
