# Update a stale rule section

## Background

`inputs/plugins/workflow-eval/rules/example.md` is a Tessl rule file
with one section, "Cache widget lookups through the cache component".
It links to `inputs/docs/recipes/example-widget-caching.md`.

## Task

The section hasn't been touched since the recipe it links to was last
edited. Update the section so it's accurate and up to date with what
the recipe currently says.
