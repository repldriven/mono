{
  "context": "The agent must rewrite example.md's single section body using only content from the linked recipe's ## Rules block. The stale body already covers 2 of 3 MUST bullets, omits both MUST NOT bullets, and contains one invented claim ('sharded per-region for latency') that appears nowhere in the recipe. Key discriminator: a generic agent asked to 'clean up' or 'improve' the section has no particular reason to question the sharding claim — it reads as a plausible detail. The skill's explicit instruction to diff against extracted source material and flag untraceable claims is what catches it.",
  "type": "weighted_checklist",
  "checklist": [
    {
      "name": "Removes or flags the untraceable sharding claim",
      "description": "The rewritten body no longer asserts that widget caches must be sharded per-region, or explicitly calls it out as untraceable/unsupported by the source recipe rather than silently keeping it",
      "max_score": 20
    },
    {
      "name": "Covers all three MUST bullets",
      "description": "The body's substance covers: caching via widget-cache/get, invalidating via widget-cache/evict! on write, and setting a TTL on every entry (never caching indefinitely)",
      "max_score": 18
    },
    {
      "name": "Covers both MUST NOT bullets",
      "description": "The body's substance covers: never reading directly from the widget store when a cached value is available, and never caching a lookup that depends on request-scoped auth context",
      "max_score": 16
    },
    {
      "name": "No content beyond the recipe",
      "description": "The rewritten body introduces no claim, function name, or constraint that isn't traceable to example-widget-caching.md's ## Rules block",
      "max_score": 14
    },
    {
      "name": "Heading and See-line unchanged",
      "description": "The section heading ('## Cache widget lookups through the cache component') and the trailing See [...] line are byte-identical to the original",
      "max_score": 10
    }
  ]
}
