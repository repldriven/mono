{;; Polylith monorepo configuration
 :source-aliases #{:dev :+bank :poly :test}
 ;; Enable kondo config copying for dependency analysis
 :copy-kondo-configs? true
 ;; Linting configuration
 :linters {:clj-kondo {:level :on}}}

;; Note: clojure-lsp only supports cljfmt for formatting, not zprint.
;; For zprint formatting, use the .zprint-format.bb script (via BufWritePost)
;; or integrate zprint via none-ls plugin.
