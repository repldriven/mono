;; This is an automatically generated config file by nvd-clojure.

;; It is only generated if you specify either the blank string "" or
;; "nvd-clojure.edn"
;; as the config filename in your CLI invocation,
;; and nvd-clojure.edn didn't exist already.

;; Feel free to tweak it, version-control it and remove any comment.

;; Configuration reference:
;; https://github.com/rm-hull/nvd-clojure/tree/v5.0.0#configuration-options

{;; You can use the `:suppression-file` in order to silence false
 ;; positives. This file will be automatically created, with whatever
 ;; filename is specified here, if it didn't exist already.
 ;; Resolved against the working directory, which is the workspace root
 ;; for `just nvd`.
 :suppression-file ".config/nvd/nvd_suppressions.xml"
 :analyzer {}}
