;;;----------------------------------------------------------------------------------
;;; Generated by protoc-gen-clojure.  DO NOT EDIT
;;;
;;; Message Implementation of package com.repldriven.mono.schemas.accounts
;;;----------------------------------------------------------------------------------
(ns com.repldriven.mono.schemas.accounts
  (:require
    [protojure.protobuf.protocol :as pb]
    [protojure.protobuf.serdes.core :as serdes.core]
    [protojure.protobuf.serdes.complex :as serdes.complex]
    [protojure.protobuf.serdes.utils :refer [tag-map]]
    [protojure.protobuf.serdes.stream :as serdes.stream]
    [com.repldriven.mono.schemas.accounts :as
     com.repldriven.mono.schemas.accounts]
    [clojure.set :as set]
    [clojure.spec.alpha :as s]))

;;----------------------------------------------------------------------------------
;;----------------------------------------------------------------------------------
;; Forward declarations
;;----------------------------------------------------------------------------------
;;----------------------------------------------------------------------------------

(declare cis->ScanAddress)
(declare ecis->ScanAddress)
(declare new-ScanAddress)
(declare cis->PaymentAddress)
(declare ecis->PaymentAddress)
(declare new-PaymentAddress)
(declare cis->Account)
(declare ecis->Account)
(declare new-Account)
(declare cis->AccountChangelog)
(declare ecis->AccountChangelog)
(declare new-AccountChangelog)

;;----------------------------------------------------------------------------------
;;----------------------------------------------------------------------------------
;; Enumerations
;;----------------------------------------------------------------------------------
;;----------------------------------------------------------------------------------

;-----------------------------------------------------------------------------
; Account-AccountStatus
;-----------------------------------------------------------------------------
(def Account-AccountStatus-default :account-status-unknown)

(def Account-AccountStatus-val2label
  {0 :account-status-unknown 1 :opening 2 :opened 3 :closing 4 :closed})

(def Account-AccountStatus-label2val
  (set/map-invert Account-AccountStatus-val2label))

(defn cis->Account-AccountStatus
  [is]
  (let [val (serdes.core/cis->Enum is)]
    (get Account-AccountStatus-val2label val val)))

(defn- get-Account-AccountStatus
  [value]
  {:pre [(or (int? value) (contains? Account-AccountStatus-label2val value))]}
  (get Account-AccountStatus-label2val value value))

(defn write-Account-AccountStatus
  ([tag value os] (write-Account-AccountStatus tag {:optimize false} value os))
  ([tag options value os]
   (serdes.core/write-Enum tag options (get-Account-AccountStatus value) os)))


;;----------------------------------------------------------------------------------
;;----------------------------------------------------------------------------------
;; PaymentAddress-identifier's oneof Implementations
;;----------------------------------------------------------------------------------
;;----------------------------------------------------------------------------------

(defn convert-PaymentAddress-identifier
  [origkeyval]
  (cond
   (get-in origkeyval [:identifier :scan])
   (update-in origkeyval [:identifier :scan] new-ScanAddress)
   (get-in origkeyval [:identifier :value])
   origkeyval
   :default
   origkeyval))

(defn write-PaymentAddress-identifier
  [identifier os]
  (let [field (first identifier)
        k (when-not (nil? field) (key field))
        v (when-not (nil? field) (val field))]
    (case k
      :scan (serdes.core/write-embedded 2 v os)
      :value (serdes.core/write-String 3 {:optimize false} v os)
      nil)))



;;----------------------------------------------------------------------------------
;;----------------------------------------------------------------------------------
;; Message Implementations
;;----------------------------------------------------------------------------------
;;----------------------------------------------------------------------------------

;-----------------------------------------------------------------------------
; ScanAddress
;-----------------------------------------------------------------------------
(defrecord ScanAddress-record [sort-code account-number]
  pb/Writer
    (serialize [this os]
      (serdes.core/write-String 1 {:optimize true} (:sort-code this) os)
      (serdes.core/write-String 2 {:optimize true} (:account-number this) os))
  pb/TypeReflection
    (gettype [this] "com.repldriven.mono.schemas.accounts.ScanAddress"))

(s/def :com.repldriven.mono.schemas.accounts.ScanAddress/sort-code string?)
(s/def :com.repldriven.mono.schemas.accounts.ScanAddress/account-number string?)
(s/def ::ScanAddress-spec
  (s/keys :opt-un
          [:com.repldriven.mono.schemas.accounts.ScanAddress/sort-code
           :com.repldriven.mono.schemas.accounts.ScanAddress/account-number]))
(def ScanAddress-defaults {:sort-code "" :account-number ""})

(defn cis->ScanAddress
  "CodedInputStream to ScanAddress"
  [is]
  (->> (tag-map ScanAddress-defaults
                (fn [tag index]
                  (case index
                    1 [:sort-code (serdes.core/cis->String is)]
                    2 [:account-number (serdes.core/cis->String is)]

                    [index (serdes.core/cis->undefined tag is)]))
                is)
       (map->ScanAddress-record)))

(defn ecis->ScanAddress
  "Embedded CodedInputStream to ScanAddress"
  [is]
  (serdes.core/cis->embedded cis->ScanAddress is))

(defn new-ScanAddress
  "Creates a new instance from a map, similar to map->ScanAddress except that
  it properly accounts for nested messages, when applicable.
  "
  [init]
  {:pre [(if (s/valid? ::ScanAddress-spec init)
           true
           (throw (ex-info "Invalid input"
                           (s/explain-data ::ScanAddress-spec init))))]}
  (-> (merge ScanAddress-defaults init)
      (map->ScanAddress-record)))

(defn pb->ScanAddress
  "Protobuf to ScanAddress"
  [input]
  (cis->ScanAddress (serdes.stream/new-cis input)))

(def ^:protojure.protobuf.any/record ScanAddress-meta
  {:type "com.repldriven.mono.schemas.accounts.ScanAddress"
   :decoder pb->ScanAddress})

;-----------------------------------------------------------------------------
; PaymentAddress
;-----------------------------------------------------------------------------
(defrecord PaymentAddress-record [scheme identifier]
  pb/Writer
    (serialize [this os]
      (serdes.core/write-String 1 {:optimize true} (:scheme this) os)
      (write-PaymentAddress-identifier (:identifier this) os))
  pb/TypeReflection
    (gettype [this] "com.repldriven.mono.schemas.accounts.PaymentAddress"))

(s/def :com.repldriven.mono.schemas.accounts.PaymentAddress/scheme string?)

(s/def ::PaymentAddress-spec
  (s/keys :opt-un
          [:com.repldriven.mono.schemas.accounts.PaymentAddress/scheme]))
(def PaymentAddress-defaults {:scheme ""})

(defn cis->PaymentAddress
  "CodedInputStream to PaymentAddress"
  [is]
  (->> (tag-map PaymentAddress-defaults
                (fn [tag index]
                  (case index
                    1 [:scheme (serdes.core/cis->String is)]
                    2 [:identifier {:scan (ecis->ScanAddress is)}]
                    3 [:identifier {:value (serdes.core/cis->String is)}]

                    [index (serdes.core/cis->undefined tag is)]))
                is)
       (map->PaymentAddress-record)))

(defn ecis->PaymentAddress
  "Embedded CodedInputStream to PaymentAddress"
  [is]
  (serdes.core/cis->embedded cis->PaymentAddress is))

(defn new-PaymentAddress
  "Creates a new instance from a map, similar to map->PaymentAddress except that
  it properly accounts for nested messages, when applicable.
  "
  [init]
  {:pre [(if (s/valid? ::PaymentAddress-spec init)
           true
           (throw (ex-info "Invalid input"
                           (s/explain-data ::PaymentAddress-spec init))))]}
  (-> (merge PaymentAddress-defaults init)
      (convert-PaymentAddress-identifier)
      (map->PaymentAddress-record)))

(defn pb->PaymentAddress
  "Protobuf to PaymentAddress"
  [input]
  (cis->PaymentAddress (serdes.stream/new-cis input)))

(def ^:protojure.protobuf.any/record PaymentAddress-meta
  {:type "com.repldriven.mono.schemas.accounts.PaymentAddress"
   :decoder pb->PaymentAddress})

;-----------------------------------------------------------------------------
; Account
;-----------------------------------------------------------------------------
(defrecord Account-record [party-id organization-id account-id currency
                           updated-at name account-status created-at
                           payment-addresses]
  pb/Writer
    (serialize [this os]
      (serdes.core/write-String 2 {:optimize true} (:party-id this) os)
      (serdes.core/write-String 8 {:optimize true} (:organization-id this) os)
      (serdes.core/write-String 1 {:optimize true} (:account-id this) os)
      (serdes.core/write-String 4 {:optimize true} (:currency this) os)
      (serdes.core/write-Int64 7 {:optimize true} (:updated-at this) os)
      (serdes.core/write-String 3 {:optimize true} (:name this) os)
      (write-Account-AccountStatus 5 {:optimize true} (:account-status this) os)
      (serdes.core/write-Int64 6 {:optimize true} (:created-at this) os)
      (serdes.complex/write-repeated serdes.core/write-embedded
                                     10
                                     (:payment-addresses this)
                                     os))
  pb/TypeReflection
    (gettype [this] "com.repldriven.mono.schemas.accounts.Account"))

(s/def :com.repldriven.mono.schemas.accounts.Account/party-id string?)
(s/def :com.repldriven.mono.schemas.accounts.Account/organization-id string?)
(s/def :com.repldriven.mono.schemas.accounts.Account/account-id string?)
(s/def :com.repldriven.mono.schemas.accounts.Account/currency string?)
(s/def :com.repldriven.mono.schemas.accounts.Account/updated-at int?)
(s/def :com.repldriven.mono.schemas.accounts.Account/name string?)
(s/def :com.repldriven.mono.schemas.accounts.Account/account-status
  (s/or :keyword keyword?
        :int int?))
(s/def :com.repldriven.mono.schemas.accounts.Account/created-at int?)

(s/def ::Account-spec
  (s/keys :opt-un [:com.repldriven.mono.schemas.accounts.Account/party-id
                   :com.repldriven.mono.schemas.accounts.Account/organization-id
                   :com.repldriven.mono.schemas.accounts.Account/account-id
                   :com.repldriven.mono.schemas.accounts.Account/currency
                   :com.repldriven.mono.schemas.accounts.Account/updated-at
                   :com.repldriven.mono.schemas.accounts.Account/name
                   :com.repldriven.mono.schemas.accounts.Account/account-status
                   :com.repldriven.mono.schemas.accounts.Account/created-at]))
(def Account-defaults
  {:party-id ""
   :organization-id ""
   :account-id ""
   :currency ""
   :updated-at 0
   :name ""
   :account-status Account-AccountStatus-default
   :created-at 0
   :payment-addresses []})

(defn cis->Account
  "CodedInputStream to Account"
  [is]
  (->> (tag-map Account-defaults
                (fn [tag index]
                  (case index
                    2 [:party-id (serdes.core/cis->String is)]
                    8 [:organization-id (serdes.core/cis->String is)]
                    1 [:account-id (serdes.core/cis->String is)]
                    4 [:currency (serdes.core/cis->String is)]
                    7 [:updated-at (serdes.core/cis->Int64 is)]
                    3 [:name (serdes.core/cis->String is)]
                    5 [:account-status (cis->Account-AccountStatus is)]
                    6 [:created-at (serdes.core/cis->Int64 is)]
                    10 [:payment-addresses
                        (serdes.complex/cis->repeated ecis->PaymentAddress is)]

                    [index (serdes.core/cis->undefined tag is)]))
                is)
       (map->Account-record)))

(defn ecis->Account
  "Embedded CodedInputStream to Account"
  [is]
  (serdes.core/cis->embedded cis->Account is))

(defn new-Account
  "Creates a new instance from a map, similar to map->Account except that
  it properly accounts for nested messages, when applicable.
  "
  [init]
  {:pre [(if (s/valid? ::Account-spec init)
           true
           (throw (ex-info "Invalid input"
                           (s/explain-data ::Account-spec init))))]}
  (-> (merge Account-defaults init)
      (cond-> (some? (get init :payment-addresses))
              (update :payment-addresses #(map new-PaymentAddress %)))
      (map->Account-record)))

(defn pb->Account
  "Protobuf to Account"
  [input]
  (cis->Account (serdes.stream/new-cis input)))

(def ^:protojure.protobuf.any/record Account-meta
  {:type "com.repldriven.mono.schemas.accounts.Account" :decoder pb->Account})

;-----------------------------------------------------------------------------
; AccountChangelog
;-----------------------------------------------------------------------------
(defrecord AccountChangelog-record [organization-id account-id status-before
                                    status-after]
  pb/Writer
    (serialize [this os]
      (serdes.core/write-String 4 {:optimize true} (:organization-id this) os)
      (serdes.core/write-String 1 {:optimize true} (:account-id this) os)
      (write-Account-AccountStatus 2 {:optimize true} (:status-before this) os)
      (write-Account-AccountStatus 3 {:optimize true} (:status-after this) os))
  pb/TypeReflection
    (gettype [this] "com.repldriven.mono.schemas.accounts.AccountChangelog"))

(s/def :com.repldriven.mono.schemas.accounts.AccountChangelog/organization-id
  string?)
(s/def :com.repldriven.mono.schemas.accounts.AccountChangelog/account-id
  string?)
(s/def :com.repldriven.mono.schemas.accounts.AccountChangelog/status-before
  (s/or :keyword keyword?
        :int int?))
(s/def :com.repldriven.mono.schemas.accounts.AccountChangelog/status-after
  (s/or :keyword keyword?
        :int int?))
(s/def ::AccountChangelog-spec
  (s/keys
   :opt-un
   [:com.repldriven.mono.schemas.accounts.AccountChangelog/organization-id
    :com.repldriven.mono.schemas.accounts.AccountChangelog/account-id
    :com.repldriven.mono.schemas.accounts.AccountChangelog/status-before
    :com.repldriven.mono.schemas.accounts.AccountChangelog/status-after]))
(def AccountChangelog-defaults
  {:organization-id ""
   :account-id ""
   :status-before Account-AccountStatus-default
   :status-after Account-AccountStatus-default})

(defn cis->AccountChangelog
  "CodedInputStream to AccountChangelog"
  [is]
  (->> (tag-map AccountChangelog-defaults
                (fn [tag index]
                  (case index
                    4 [:organization-id (serdes.core/cis->String is)]
                    1 [:account-id (serdes.core/cis->String is)]
                    2 [:status-before (cis->Account-AccountStatus is)]
                    3 [:status-after (cis->Account-AccountStatus is)]

                    [index (serdes.core/cis->undefined tag is)]))
                is)
       (map->AccountChangelog-record)))

(defn ecis->AccountChangelog
  "Embedded CodedInputStream to AccountChangelog"
  [is]
  (serdes.core/cis->embedded cis->AccountChangelog is))

(defn new-AccountChangelog
  "Creates a new instance from a map, similar to map->AccountChangelog except that
  it properly accounts for nested messages, when applicable.
  "
  [init]
  {:pre [(if (s/valid? ::AccountChangelog-spec init)
           true
           (throw (ex-info "Invalid input"
                           (s/explain-data ::AccountChangelog-spec init))))]}
  (-> (merge AccountChangelog-defaults init)
      (map->AccountChangelog-record)))

(defn pb->AccountChangelog
  "Protobuf to AccountChangelog"
  [input]
  (cis->AccountChangelog (serdes.stream/new-cis input)))

(def ^:protojure.protobuf.any/record AccountChangelog-meta
  {:type "com.repldriven.mono.schemas.accounts.AccountChangelog"
   :decoder pb->AccountChangelog})

